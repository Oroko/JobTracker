      
         <% if  object.errors.any? %>
          <div class="content notification is-danger global-notification"">
            <strong class="is-medium"><%= pluralize( object.errors.count, "error") %> prohibited this object from being saved:</strong>

            <ul>
              <%  object.errors.each do |error| %>
                <li> <%= error.full_message %> </li>
              <% end %>
            </ul>
          </div>
      <% end %>
      
      
      
      
      
     